<%= turbo_frame_tag "general_modal" do %>

    <div class="modal fade" id="shareRecipeModal" tabindex="-1" aria-labelledby="newGeneralModalLabel" aria-hidden="true" data-controller="general-modal" data-action="turbo:before-render@document->general-modal#hideBeforeRender">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content" id="modal_content">
                <div class="modal-header">
                    <h4 class="modal-title" id="newGeneralModalLabel"><%= title %></h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>

                <div class="modal-body general_modal_body" id="general_modal_body">
                    <%= yield %>
                </div>
            </div>
        </div>
    </div>

<% end %>