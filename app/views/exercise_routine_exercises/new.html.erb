<%= render "layouts/general_modal", title: @exercise ? "Add #{@exercise.name} to Routine": "Add exercise  to #{@exercise_routine.name}" do %>
    <% if @exercise %>
        <%= render "choose_routine" %>
    <% else %>
        <%= render "choose_exercise" %>
    <% end %>
<% end %>